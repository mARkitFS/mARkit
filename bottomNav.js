import { createMaterialBottomTabNavigator } from 'react-navigation-material-bottom-tabs';

// create tabs - one for each dashboard, has to include all the screens accessible from that dashboard (and in total, everything that's currently imported in switch nav)
